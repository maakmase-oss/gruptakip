"use client";
import {useState} from "react";
import {useRouter} from "next/navigation";
export default function Login(){const [user,setUser]=useState(""),[password,setPassword]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");const router=useRouter();async function submit(e){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({user,password})}),d=await r.json();if(!r.ok){setError(d.error||"Giriş başarısız");return}router.replace("/");router.refresh()}finally{setBusy(false)}}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20}}><form onSubmit={submit} className="panel" style={{width:"100%",maxWidth:420}}><span className="brand">GRUPTAKİP</span><h1>Panel Girişi</h1><p>Yetkili kullanıcı hesabınızla giriş yapın.</p><input autoFocus autoComplete="username" value={user} onChange={e=>setUser(e.target.value)} placeholder="Kullanıcı adı" style={{width:"100%",marginBottom:10}}/><input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Şifre" style={{width:"100%",marginBottom:10}}/>{error&&<p style={{fontWeight:700}}>{error}</p>}<button disabled={busy} style={{width:"100%"}}>{busy?"Giriş yapılıyor...":"Giriş Yap"}</button></form></main>}
